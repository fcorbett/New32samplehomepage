# Block direct web access to secrets and support files
<Files "config.php">
  Require all denied
</Files>

<Files "config.sample.php">
  Require all denied
</Files>

# Only allow the contact endpoint as a script; deny listing
Options -Indexes
